

logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.env
.env.test

# temp files
TESTS-DESIGN.md
FEATURES-PLAN.md
AUDIT-FEATURES.md

dist
dist-ssr
coverage
playwright-report/
test-results/
*.local
uploads/

# Generated static lesson content (see scripts/generate-static-content.ts)
public/lessons/
public/curriculum.json

# Claude Code local state (worktrees, local settings)
.claude/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.runs
.direnv/

# Self-hosted focus-radio audio (large binary, deployed manually to the server).
# Lives outside public/ so Vite doesn't copy it into dist/ on every build;
# served by nginx in prod and by Express (media/radio static mount) in dev.
media/
