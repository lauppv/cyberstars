#include <stdio.h>
#include <stdlib.h>

int main() {
    int n;
    scanf("%d", &n);
    // TODO: allocate an array of n ints with malloc
    // TODO: read n numbers into the array
    // TODO: compute and print the sum
    // TODO: compute and print the average with 2 decimal places
    // TODO: free the allocated memory
    return 0;
}