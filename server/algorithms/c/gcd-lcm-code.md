#include <stdio.h>

int main(void) {
    int a, b;
    scanf("%d %d", &a, &b);
    // TODO: save original values of a and b
    // TODO: compute GCD using Euclid's algorithm
    // TODO: compute LCM = original_a / gcd * original_b
    // TODO: print "GCD: X" and "LCM: Y"
    return 0;
}
