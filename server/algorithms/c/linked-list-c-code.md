#include <stdio.h>
#include <stdlib.h>
#include <string.h>

struct Node {
    int data;
    struct Node *next;
};

// TODO: write a function to insert a value at the end of the list
// TODO: write a function to print the list

int main() {
    struct Node *head = NULL;
    int n;
    scanf("%d", &n);
    // TODO: read n commands and execute them
    // For "INSERT x": insert x at the end of the linked list
    // For "PRINT": print all elements or "EMPTY" if list is empty
    return 0;
}