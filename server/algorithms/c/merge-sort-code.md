#include <stdio.h>
#include <stdlib.h>

// TODO: write a merge function that merges two sorted halves
// void merge(int arr[], int left, int mid, int right) { ... }

// TODO: write mergeSort that recursively sorts
// void mergeSort(int arr[], int left, int right) { ... }

int main() {
int n;
scanf("%d", &n);
int arr[1000];
// TODO: read n numbers into arr
// TODO: call mergeSort
// TODO: print the sorted array space-separated
return 0;
}
