#include <stdio.h>
#include <stdlib.h>
#include <string.h>

struct Node {
int data;
struct Node \*next;
};

// TODO: scrie o funcție care inserează o valoare la finalul listei
// TODO: scrie o funcție care afișează lista

int main() {
struct Node \*head = NULL;
int n;
scanf("%d", &n);
// TODO: citește n comenzi și execută-le
// Pentru "INSERT x": inserează x la finalul listei înlănțuite
// Pentru "PRINT": afișează toate elementele sau "EMPTY" dacă lista este goală
return 0;
}
