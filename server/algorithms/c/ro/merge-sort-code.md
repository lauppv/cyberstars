#include <stdio.h>
#include <stdlib.h>

// TODO: scrie o funcție merge care interclasează două jumătăți sortate
// void merge(int arr[], int left, int mid, int right) { ... }

// TODO: scrie mergeSort care sortează recursiv
// void mergeSort(int arr[], int left, int right) { ... }

int main() {
int n;
scanf("%d", &n);
int arr[1000];
// TODO: citește n numere în arr
// TODO: apelează mergeSort
// TODO: afișează tabloul sortat separat prin spații
return 0;
}
