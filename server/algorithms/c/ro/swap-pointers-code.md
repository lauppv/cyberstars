#include <stdio.h>

// TODO: scrie o funcție swap care primește doi pointeri la int
// void swap(int *a, int *b) { ... }

int main() {
int x, y;
scanf("%d %d", &x, &y);
// TODO: apelează swap(&x, &y)
printf("%d %d\n", x, y);
return 0;
}
