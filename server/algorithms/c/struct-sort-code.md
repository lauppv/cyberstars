#include <stdio.h>
#include <string.h>

struct Student {
char name[51];
int grade;
};

int main() {
int n;
scanf("%d", &n);
struct Student students[50];
// TODO: read n students (name and grade)
// TODO: sort students by grade in ascending order
// TODO: print each student's name and grade
return 0;
}
