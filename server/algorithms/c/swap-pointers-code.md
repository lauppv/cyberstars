#include <stdio.h>

// TODO: write a swap function that takes two int pointers
// void swap(int *a, int *b) { ... }

int main() {
int x, y;
scanf("%d %d", &x, &y);
// TODO: call swap(&x, &y)
printf("%d %d\n", x, y);
return 0;
}
