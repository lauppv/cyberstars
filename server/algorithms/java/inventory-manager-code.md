import java.util.Scanner;
import java.util.HashMap;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        HashMap<String, Integer> inventory = new HashMap<>();
        // TODO: read number of commands
        // TODO: for each command:
        //   "add item qty" — add to inventory
        //   "remove item qty" — remove from inventory (print "Not enough ITEM" if insufficient)
        //   "check item" — print "ITEM: QTY"
        // TODO: print "Items: N" (count of items with qty > 0)
    }
}
