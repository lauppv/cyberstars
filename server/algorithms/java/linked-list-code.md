import java.util.Scanner;

class Node {
    int value;
    Node next;
    // TODO: constructor
}

class LinkedList {
    Node head;

    // TODO: add(int value) — add to end of list
    // TODO: remove(int value) — remove first occurrence, return false if not found
    // TODO: print() — print "val1 -> val2 -> ..." or "Empty"
}

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        LinkedList list = new LinkedList();
        // TODO: read number of commands
        // TODO: process add/remove/print commands
        // TODO: for remove, print "Not found" if value not in list
    }
}
