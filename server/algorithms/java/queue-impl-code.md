import java.util.Scanner;
import java.util.ArrayList;

class Queue {
    private ArrayList<Integer> data = new ArrayList<>();

    // TODO: enqueue(int value) — add to back
    // TODO: dequeue() — remove and return front, or return -1 if empty
    // TODO: peek() — return front without removing, or return -1 if empty
    // TODO: isEmpty() — check if queue is empty
}

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        Queue queue = new Queue();
        // TODO: read number of commands
        // TODO: process enqueue/dequeue/peek commands
        // TODO: for dequeue and peek, print "Empty" if queue is empty
    }
}
