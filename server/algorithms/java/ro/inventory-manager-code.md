import java.util.Scanner;
import java.util.HashMap;

public class Main {
public static void main(String[] args) {
Scanner sc = new Scanner(System.in);
HashMap<String, Integer> inventory = new HashMap<>();
// TODO: citește numărul de comenzi
// TODO: pentru fiecare comandă:
// "add item qty" — adaugă în inventar
// "remove item qty" — elimină din inventar (afișează "Not enough ITEM" dacă insuficient)
// "check item" — afișează "ITEM: QTY"
// TODO: afișează "Items: N" (numărul de articole cu qty > 0)
}
}
