import java.util.Scanner;
import java.util.Iterator;

class NumberRange implements Iterable<Integer> {
// TODO: adaugă câmpurile start și end
// TODO: constructor

    // TODO: implementează metoda iterator() care returnează un Iterator<Integer>
    // Iteratorul ar trebui să meargă de la start la end (inclusiv)

    @Override
    public Iterator<Integer> iterator() {
        // TODO: returnează un nou Iterator<Integer> cu hasNext() și next()
        return null;
    }

}

public class Main {
public static void main(String[] args) {
Scanner sc = new Scanner(System.in);
// TODO: citește start și end
// TODO: creează NumberRange
// TODO: folosește bucla for-each pentru a afișa fiecare număr
}
}
