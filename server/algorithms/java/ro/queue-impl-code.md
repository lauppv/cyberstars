import java.util.Scanner;
import java.util.ArrayList;

class Queue {
private ArrayList<Integer> data = new ArrayList<>();

    // TODO: enqueue(int value) — adaugă la spate
    // TODO: dequeue() — elimină și returnează elementul din față, sau returnează -1 dacă este goală
    // TODO: peek() — returnează elementul din față fără a-l elimina, sau returnează -1 dacă este goală
    // TODO: isEmpty() — verifică dacă coada este goală

}

public class Main {
public static void main(String[] args) {
Scanner sc = new Scanner(System.in);
Queue queue = new Queue();
// TODO: citește numărul de comenzi
// TODO: procesează comenzile enqueue/dequeue/peek
// TODO: pentru dequeue și peek, afișează "Empty" dacă coada este goală
}
}
