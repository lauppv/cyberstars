import java.util.Scanner;
import java.util.ArrayList;

class Stack {
private ArrayList<Integer> data = new ArrayList<>();

    // TODO: push(int value) — add to top
    // TODO: pop() — remove and return top, or return -1 if empty
    // TODO: peek() — return top without removing, or return -1 if empty
    // TODO: isEmpty() — check if stack is empty

}

public class Main {
public static void main(String[] args) {
Scanner sc = new Scanner(System.in);
Stack stack = new Stack();
// TODO: read number of commands
// TODO: process push/pop/peek commands
// TODO: for pop and peek, print "Empty" if stack is empty
}
}
