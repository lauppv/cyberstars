#include <stdio.h>

int main(void) {
int heroes[3];

    // 1. set heroes[0] to 10
    // 2. set heroes[1] to 20
    // 3. set heroes[2] to 30
    // 4. print the size with sizeof(heroes) / sizeof(heroes[0])
    // 5. print the first element
    // 6. print the last element

    return 0;

}
