#include <stdio.h>

int main(void) {
int scores[] = { 80, 95, 60, 72, 88 };
int n = sizeof(scores) / sizeof(scores[0]);

    // 1. print each score on its own line
    // 2. print the total
    // 3. print the average (cast to double)

    return 0;

}
