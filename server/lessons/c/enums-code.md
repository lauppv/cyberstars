#include <stdio.h>

enum Season { SPRING, SUMMER, AUTUMN, WINTER };

void printSeason(enum Season s) {
    // use switch to print the name
}

int main(void) {
    printSeason(SPRING);
    printSeason(SUMMER);
    printSeason(AUTUMN);
    printSeason(WINTER);
    return 0;
}
