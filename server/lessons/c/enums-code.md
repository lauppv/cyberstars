#include <stdio.h>

enum Season { SPRING, SUMMER, AUTUMN, WINTER };

void printSeason(enum Season s) {
}

int main(void) {
    printSeason(SPRING);
    printSeason(SUMMER);
    printSeason(AUTUMN);
    printSeason(WINTER);
    return 0;
}
