#include <stdio.h>

enum Season { SPRING, SUMMER, AUTUMN, WINTER };

void print_season(enum Season s) {
}

int main(void) {
    print_season(SPRING);
    print_season(SUMMER);
    print_season(AUTUMN);
    print_season(WINTER);
    return 0;
}
