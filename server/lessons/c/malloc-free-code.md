#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int n = 5;
    int values[] = {2, 4, 6, 8, 10};

    // 1. allocate a dynamic array of n ints with malloc
    // 2. fill it with the values above
    // 3. print each value on its own line
    // 4. free the memory

    return 0;
}
