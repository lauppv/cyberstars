#include <stdio.h>
#include <pthread.h>

void *repeat(void *arg) {
    // print the string 3 times
    return NULL;
}

int main(void) {
    pthread_t t1, t2;

    // create threads and join them

    printf("Game over\n");
    return 0;
}
