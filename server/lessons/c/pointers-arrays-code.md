#include <stdio.h>

int sumArray(int *ptr, int n) {
    int total = 0;
    return total;
}

int main(void) {
    int nums[] = {5, 10, 15, 20};
    int result = sumArray(nums, 4);
    printf("%d\n", result);
    return 0;
}
