#include <stdio.h>

int sum_array(int *ptr, int n) {
    int total = 0;
    return total;
}

int main(void) {
    int nums[] = {5, 10, 15, 20};
    int result = sum_array(nums, 4);
    printf("%d\n", result);
    return 0;
}
