#include <stdio.h>

int suma_tablou(int *ptr, int n) {
    int total = 0;
    return total;
}

int main(void) {
    int numere[] = {5, 10, 15, 20};
    int rezultat = suma_tablou(numere, 4);
    printf("%d\n", rezultat);
    return 0;
}
