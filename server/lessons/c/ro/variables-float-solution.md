```c
#include <stdio.h>

int main(void) {
    double temperatura, umiditate;
    scanf("%lf %lf", &temperatura, &umiditate);

    printf("Temperatura: %.2f\n", temperatura);
    printf("Umiditate: %.2f\n", umiditate);
    return 0;
}
```
