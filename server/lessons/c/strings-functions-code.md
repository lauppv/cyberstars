#include <stdio.h>
#include <string.h>

int main(void) {
char first[] = "Cyber";
char second[] = "Stars";

    // print length of first
    // copy first into result, concatenate second, print result

    return 0;

}
