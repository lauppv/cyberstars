#include <stdio.h>
#include <string.h>

// define struct Car with: brand (char[50]), year (int), km (int)

int main(void) {
    // create two cars and print them
    // format: "Brand (Year) - Km km"

    return 0;
}
