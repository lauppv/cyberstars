#include <stdio.h>

// define typedef struct CrewMember with: name (char[50]), rank (int), rating (double)

int main(void) {
    // create array of 3 crew members and print them
    // format: "Name - Rank X - Rating Y.YY"

    return 0;
}
