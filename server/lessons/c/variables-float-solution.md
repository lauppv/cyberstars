```c
#include <stdio.h>

int main(void) {
    double temperature, humidity;
    scanf("%lf %lf", &temperature, &humidity);

    printf("Temperature: %.2f\n", temperature);
    printf("Humidity: %.2f\n", humidity);
    return 0;
}
```
