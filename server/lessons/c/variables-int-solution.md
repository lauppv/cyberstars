```c
#include <stdio.h>

int main(void) {
    int age, access_level;
    scanf("%d %d", &age, &access_level);

    printf("Age: %d\n", age);
    printf("Access: %d\n", access_level);
    return 0;
}
```
