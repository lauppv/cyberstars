class BankAccount {
    private int balance;

    // Write a constructor that takes starting balance

    // Write getBalance() that returns balance

    // Write deposit(int amount) — only add if amount > 0

    // Write withdraw(int amount) — only subtract if amount > 0 and amount <= balance

}

public class Main {
    public static void main(String[] args) {
        // Create account with 1000
        // Deposit 500
        // Withdraw 200
        // Try to withdraw 2000 (should fail silently)
        // Print the balance
    }
}
