class Shape {
    double area() {
        return 0;
    }
}

// Create a Circle class that extends Shape
// It should have a radius field and a constructor
// Override area() to return Math.PI _ radius _ radius

// Create a Rectangle class that extends Shape
// It should have width and height fields and a constructor
// Override area() to return width \* height

public class Main {
    public static void main(String[] args) {
        // Create a Circle with radius 5
        // Create a Rectangle with width 4 and height 6
        // Print: "Circle area: " + formatted area (2 decimal places)
        // Print: "Rectangle area: " + area
    }
}
