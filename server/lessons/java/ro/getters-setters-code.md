class ContBancar {
    private int sold;

    // Scrie un constructor care primește soldul inițial

    // Scrie getSold() care returnează sold

    // Scrie depune(int suma) — adaugă doar dacă suma > 0

    // Scrie retrage(int suma) — scade doar dacă suma > 0 și suma <= sold

}

public class Main {
    public static void main(String[] args) {
        // Creează un cont cu 1000
        // Depune 500
        // Retrage 200
        // Încearcă să retragi 2000 (ar trebui să eșueze în tăcere)
        // Afișează soldul (sold)
    }
}
