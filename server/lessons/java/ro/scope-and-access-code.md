class Portofel {
    private int bani;

    // Scrie un constructor public care primește banii de start

    // Scrie o metodă public adaugaBani(int suma) — doar dacă suma > 0

    // Scrie o metodă public cheltuieBani(int suma) — doar dacă suma > 0 și suma <= bani

    // Scrie o metodă public getSold() care returnează bani

}

public class Main {
    public static void main(String[] args) {
        // Creează portofel cu 100
        // Adaugă 50
        // Cheltuie 30
        // Încearcă să cheltui 200 (ar trebui să eșueze)
        // Afișează soldul
    }
}
