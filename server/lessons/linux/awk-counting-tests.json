{
  "checks": [
    {
      "kind": "command_output",
      "cmd": "cat flight-deck/total-weight.txt",
      "expected": "610"
    },
    { "kind": "dir_exists", "path": "flight-deck" }
  ],
  "requires": [
    { "kind": "command", "pattern": "awk\\b.*\\$2.*cargo_weight\\.txt" },
    { "kind": "command", "pattern": "awk\\b.*\\$2\\s*>\\s*100" },
    { "kind": "command", "name": "mkdir" },
    { "kind": "command", "name": "mv" }
  ]
}
