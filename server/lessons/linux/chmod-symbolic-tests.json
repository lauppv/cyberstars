{
  "checks": [
    {
      "kind": "command_output",
      "cmd": "stat -c %A deploy.sh | cut -c4",
      "expected": "x"
    }
  ],
  "requires": [
    {
      "kind": "command",
      "name": "chmod"
    }
  ]
}
