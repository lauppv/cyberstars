{
  "checks": [
    {
      "kind": "command_output",
      "cmd": "cat command-center/distress.txt",
      "expected": "mayday: engine failure pod 9\nmayday: oxygen leak sector 4\nmayday: collision warning asteroid belt"
    },
    { "kind": "dir_exists", "path": "command-center" }
  ],
  "requires": [
    { "kind": "command", "pattern": "cat\\b.*transmissions\\.log\\s*\\|\\s*grep\\b.*mayday" },
    { "kind": "command", "name": "mkdir" },
    { "kind": "command", "name": "mv" },
    { "kind": "command", "pattern": "wc\\b.*-l" }
  ]
}
