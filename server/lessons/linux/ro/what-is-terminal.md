Un **terminal** este o fereastră text în care comunici cu calculatorul **scriind
comenzi** în loc să apeși butoane.

Scrii o comandă, apeși **Enter**, iar calculatorul o execută și îți arată
rezultatul. Simbolul mic de la începutul liniei (de obicei `$`) se numește
**prompt** — înseamnă că sistemul este pregătit și te așteaptă.

Prima comandă pe care o vom învăța este **echo**. Ea afișează pur și simplu textul
pe care i-l dai.

```bash
echo Hello
```

Va afișa:

```text
Hello
```

Poți afișa orice text dorești:

```bash
echo Welcome to Linux
```

```text
Welcome to Linux
```

Dacă textul trebuie să rămână exact așa cum este scris (cu spații), îl poți pune
între ghilimele:

```bash
echo "Linux is fun"
```

```text
Linux is fun
```

Nu-ți face griji că trebuie să memorezi totul — terminalul este un loc unde poți
**experimenta**. Încearcă o comandă, vezi ce se întâmplă, încearcă alta.

---

## Misiune: Prima Transmisiune

Sistemul de comunicații al stației tocmai a fost activat și are nevoie de un test de semnal. Sarcina ta este să trimiți primul mesaj de la acest terminal.

Folosește comanda `echo` pentru a transmite exact mesajul: `Mission control online`

**Rezultat așteptat**

Terminalul afișează `Mission control online`, confirmând că transmisiunea ta a fost recepționată.
