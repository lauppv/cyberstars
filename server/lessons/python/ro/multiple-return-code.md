def parse(reading): # împarte citirea, convertește în numere, returnează suma, cel mai mare, cel mai mic
pass

reading = input()
total, largest, smallest = parse(reading)
print(f"Total: {total}")
print(f"Cel mai mare: {largest}")
print(f"Cel mai mic: {smallest}")
