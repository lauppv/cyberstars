codes = ["racecar", "signal", "level", "orbit", "radar"]

def is_palindrome(cuvant): # returnează True dacă cuvant se citește la fel de la stânga la dreapta și invers (folosește o buclă, fără [::-1])
pass

# verifică fiecare cod, afișează "cod: True/False", apoi afișează "Semnale valide: " + numărul
