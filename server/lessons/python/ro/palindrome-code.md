coduri = ["racecar", "semnal", "nivel", "orbit", "radar"]

def este_palindrom(cuvant): # returnează True dacă cuvant se citește la fel de la stânga la dreapta și invers (folosește o buclă, fără [::-1])
    pass

# verifică fiecare cod, afișează "cod: True/False", apoi afișează "Semnale valide: " + numărul
