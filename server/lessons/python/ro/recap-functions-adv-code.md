readings = ["10", "bad", "25", "7", "x"]

def safe_int(text, fallback=0): # încearcă să convertești text în int, returnează fallback dacă eșuează
pass

def summarize(numere): # returnează total, cel mai mare, cel mai mic
pass

# construiește numere cu safe_int, afișează "Numere:" și lista, apoi summarize și afișează "Total:", "Cel mai mare:", "Cel mai mic:"
