fuels = [1, 2, 3, 4, 5, 6, 7, 8, 9]

def pair_with_sum(numbers, target): # use two pointers to find all pairs that add up to target
pass

# call pair_with_sum(fuels, 10), then print "Pairs: " + the list and "Total pairs: " + the count
